interface:
  display_name: "Multi Harness"
  short_description: "Explicitly route work to Pi or OpenCode"
  default_prompt: "Use $multi-harness to delegate a scoped code review explicitly to Pi or OpenCode."
